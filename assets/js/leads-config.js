/* TrueNest lead endpoint config.
 *
 * After deploying the app/ to Vercel, paste the deployment's leads API
 * URL below, e.g.:
 *   window.TRUENEST_LEADS_ENDPOINT = "https://truenest-xyz.vercel.app/api/leads";
 *
 * While this is left empty, forms still work but only show the
 * thank-you message (no data is stored) — so the live site never breaks
 * before the backend exists.
 */
window.TRUENEST_LEADS_ENDPOINT = "https://truenest-website-8lbx.vercel.app/api/leads";
